// import { twMerge } from 'tailwind-merge';

// const Pill = ({ type, label, appendIcon, className, dropdown = null }) => {
// 	return (
// 		<div
// 			className={twMerge(
// 				'flex justify-center items-center gap-1 py-0.5 px-1 mr-1 rounded-full border border-black text-black',
// 				type === 'fill' && 'border-none bg-primary-main text-white',
// 				type === 'outline' && 'border-primary-main bg-inherit text-primary-main',
// 				className,
// 			)}
// 		>
// 			<p className={twMerge('font-medium text-xs text-center')}>{label}</p>
// 			{appendIcon && appendIcon}
// 			{dropdown && dropdown}
// 		</div>
// 	);
// };

// export default Pill;